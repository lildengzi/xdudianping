<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sens.mapper.NoteCommentMapper">


    <select id="countByUserId" resultType="java.lang.Integer">
        SELECT count(*)
        FROM biz_note_comment
        WHERE note_id IN (SELECT id FROM biz_note WHERE user_id = #{userId})
    </select>

    <select id="selectByUserId" resultType="com.example.sens.entity.NoteComment">
        select * from biz_note_comment
        <where>
            1 = 1
            <if test="userId != -1">
                AND (user_id = #{userId} or note_id
                in (select id from biz_note where user_id = #{userId}))
            </if>
            <if test="content != null and content != ''">
                AND content like concat('%', #{content}, '%')
            </if>
        </where>
    </select>
</mapper>